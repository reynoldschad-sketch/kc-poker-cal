{
  "name": "poker-radar-kc",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "dev": "npm run build && npx wrangler pages dev public",
    "build:dragdrop": "node build.mjs && node build-standalone.mjs"
  }
}
